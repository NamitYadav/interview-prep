import type { Question } from '../types';

export const design: Question[] = [
  // Product surfaces (13)
  {
    id: 'design-001',
    round: 'design',
    category: 'Product surfaces',
    question: 'Design a social feed that infinite-scrolls through mixed content types: text posts, images, and video previews.',
    answer: [
      "Start with requirements, not layout: expected feed size, refresh cadence (pull-to-refresh, live insert of new items above the fold), and whether ordering is chronological or ranked, since ranked feeds need a different data contract (a stable cursor, not a simple offset).",
      "Cover the data layer: paginate with a cursor, not page numbers, so inserts and deletes upstream do not shift your window; prefetch the next page before the user hits the bottom, and cache pages so back-navigation is instant.",
      "Cover rendering: virtualize the list once it is long — only mount DOM for the visible window plus a buffer — and treat each content type as its own component behind a stable, type-based renderer so adding a new post type never touches the list itself.",
      "Close with the failure modes: images and video must not block text from rendering (lazy-load with a placeholder sized to avoid layout shift), and a failed page fetch should retry in place, not blank the whole feed.",
      "Close on the non-functionals, because this is a surface you measure in the field and not in the lab: RUM for INP and CLS on the feed itself, an error budget for failed page fetches, virtualization shipped behind a flag against the current list, and an explicit rollback trigger — a scroll-jank or fetch-error regression past the budget means revert, not a follow-up ticket.",
    ],
    keyPoints: [
      'Cursor-based pagination, not offset, to survive upstream inserts/deletes',
      'List virtualization once the feed is long',
      'Content types render behind a stable, type-based component switch',
      'Failed page fetch retries in place; media does not block text or shift layout',
      'Names RUM metrics, a flagged rollout, and a concrete rollback trigger rather than stopping at the design',
    ],
    followUps: ['How would you insert a new post at the top without disrupting the user mid-scroll?', 'How do you test that virtualization does not break screen reader navigation?'],
  },
  {
    id: 'design-002',
    round: 'design',
    category: 'Product surfaces',
    question: 'Design a checkout flow spanning cart, address, payment, and confirmation, that must survive a flaky mobile connection.',
    answer: [
      "Lead with the requirement that actually shapes this: no lost cart state and no double-charged payment, since a flaky connection means requests can silently fail or silently succeed after the client gives up waiting.",
      "Cover state ownership: keep cart and form state client-side and persist it (so a dropped connection or refresh does not lose the user's progress), but treat the payment step itself as server-authoritative with an idempotency key generated client-side, so a retried submit cannot create a duplicate charge.",
      "Cover the network layer directly: show a distinct 'still trying' state instead of a generic spinner past a short timeout, retry idempotent requests automatically, and never retry the payment submit automatically — surface it and let the user confirm, since automatic retry there is exactly how double charges happen.",
      "Cover PSD2 strong customer authentication explicitly, because in the EU it is the step that actually breaks checkouts: a card payment can trigger a 3-D Secure challenge that either redirects the browser away to the issuer's ACS page or renders inline in a challenge iframe. Persist the payment intent id before you hand control away, so when the user comes back — possibly minutes later, possibly into a cold-started tab — the client resumes from that intent rather than from in-memory state, and the resume call is idempotent so a double return cannot double-charge.",
      "Close with confirmation: do not treat 'payment request sent' as 'payment succeeded' — poll or use a webhook-backed status check before showing the success screen, so the UI never lies about a charge that is still pending. Returning from the issuer's page is not success either — the success screen waits on your server's confirmed authorisation status, nothing else.",
      "Close on the non-functionals. Accessibility is a legal floor here, not a nicety: consumer e-commerce checkout is in scope for the European Accessibility Act, transposed in Germany as the BFSG and in force since June 2025, so WCAG 2.2 AA — the level the European standard EN 301 549 tracks — is a compliance requirement, and payment forms and error messaging are exactly what gets audited. Then instrument completion rate per step and payment-failure rate, ship changes behind a flag, and make a rise in failed or duplicate authorisations the rollback trigger.",
    ],
    keyPoints: [
      'Idempotency key on the payment submit to prevent duplicate charges',
      'Cart/form state persisted client-side; payment step is server-authoritative',
      'Payment submit is never auto-retried; other requests can be',
      'Success screen only shown after a confirmed status, not just a sent request',
      'Names PSD2 SCA / 3-D Secure and the redirect-vs-iframe challenge',
      'Persists the payment intent id so the return from the issuer ACS page resumes idempotently, not from memory',
      'Names WCAG 2.2 AA via the EAA/BFSG as a compliance floor, plus a payment-failure rollback trigger',
    ],
    followUps: ['How would you handle a payment that is stuck in a pending state for minutes?', 'What would you show if the address-validation service itself is down?'],
  },
  {
    id: 'design-003',
    round: 'design',
    category: 'Product surfaces',
    question: 'Design a dashboard rendering live, high-frequency data (e.g. sensor or trading updates) without freezing the tab.',
    answer: [
      "Separate two problems up front: how fast the data arrives (a WebSocket or SSE stream, easily hundreds of updates a second) and how fast the UI can usefully redraw. Put real numbers on the second one before designing anything: 60fps is a 16ms frame, the target I would hold is tick-to-paint under 100ms from message arrival to pixels, INP under 200ms for the controls the user touches, and a dropped-frame rate near zero during a burst — most of a frame goes to layout and paint, not your logic.",
      "Buffer incoming updates and batch them: coalesce a burst of ticks into the latest value per key, and flush the batch on a fixed interval (e.g. every animation frame) rather than re-rendering on every single message.",
      "Push the hot path out of React's render cycle where it matters: for something like a scrolling ticker or a chart, write directly to a canvas or update a ref-driven DOM node, and reserve React state for things the user actually interacts with (filters, selected row).",
      "Cover backpressure: if the tab is backgrounded or the consumer falls behind, drop or coalesce further rather than queueing unboundedly, and reconnect the stream with backoff on disconnect rather than silently going stale.",
      "Close on the non-functionals: report tick-to-paint p95 and dropped frames as real RUM metrics rather than eyeballing smoothness, alert on feed staleness, ship the batched render path behind a flag with the previous path as the rollback, and roll back on a latency-budget or error-budget breach.",
    ],
    keyPoints: [
      'Coalesces high-frequency updates into a batched flush, not one render per message',
      'Keeps the hottest path (e.g. a chart) outside React state where it matters',
      'Has an explicit backpressure/drop strategy instead of unbounded queueing',
      'Reconnects a dropped stream with backoff rather than going silently stale',
      'Names explicit budgets (tick-to-paint under 100ms, INP under 200ms) instead of qualitative speed claims',
      'Measures those budgets in RUM and names a rollback trigger for a breach',
    ],
    followUps: ['How would you test that the batching does not drop the final value in a burst?', 'How would you show the user that the stream just reconnected after a gap?'],
  },
  {
    id: 'design-004',
    round: 'design',
    category: 'Product surfaces',
    question: 'Design a global search box with autocomplete across multiple backend data sources.',
    answer: [
      "Start with the UX constraint that drives the architecture: results need to feel instant, but you are fanning out to multiple sources (e.g. products, docs, people) that will not all respond at the same speed.",
      "Debounce the keystroke, not the request: fire on a short debounce (150-250ms) but also cancel the in-flight request for the previous query the moment a new one is fired, so a slow stale response can never overwrite a newer one — key results by the query that produced them and discard anything that does not match the current input.",
      "Render sources independently: each source's results appear in its own section as soon as it resolves, rather than waiting for the slowest one to show anything at all, and give each source a visible loading and empty state.",
      "Cover ranking and keyboard behavior: merge or interleave sections by a documented priority (not arbitrary arrival order), and make sure arrow-key navigation moves predictably across sections, since that is the first thing a power user will test.",
      "Close on the non-functionals: hold p95 keystroke-to-first-result under 300ms and INP under 200ms on the input, measure both in RUM broken down per source so one slow backend is visible rather than reported, ship a ranking change behind a flag, and roll back on a timeout-rate spike or a drop in result selection.",
    ],
    keyPoints: [
      'Cancels in-flight requests for stale queries; results keyed by the query that produced them',
      'Each data source renders independently as it resolves, not gated on the slowest',
      'Documented section ordering/ranking, not arrival order',
      'Keyboard navigation works predictably across sections',
      'Names a latency budget (p95 keystroke-to-first-result under 300ms) measured per source, with a rollback trigger',
    ],
    followUps: ['How would you highlight the matched substring safely across sources with different text formats?', 'How would you cache recent queries without serving stale results?'],
  },
  {
    id: 'design-005',
    round: 'design',
    category: 'Product surfaces',
    question: 'Design a notification center that must stay in sync across open tabs and devices in near real time.',
    answer: [
      "Name the sync boundary first: cross-tab (same browser, same device) is a different problem from cross-device, and conflating them leads to overbuilding — cross-tab can use BroadcastChannel or a shared storage event; cross-device needs a server round trip regardless.",
      "Design the source of truth as server-driven: the client never marks something read purely locally without confirming with the server, or two tabs will disagree about unread count the moment one of them acts.",
      "Use a push channel (WebSocket/SSE) for new notifications and read-state changes, with a REST fallback poll if the push channel drops, so the badge count never silently goes stale for a user on a bad connection.",
      "Cover the ordering edge case: a 'mark all as read' from one tab or device must not race with a new notification arriving mid-action — sequence server-side by a monotonic event id, not client timestamps, so out-of-order delivery cannot resurrect a notification that was just cleared.",
      "Close on the non-functionals: instrument push-channel connect success, reconnect rate, and how often clients fall back to polling; sample-check the rendered badge count against the server and alert on divergence; roll the push channel out behind a flag with polling as the standing fallback, and roll back on a rise in missed or duplicated notifications.",
    ],
    keyPoints: [
      'Distinguishes cross-tab sync from cross-device sync as different mechanisms',
      'Server is the source of truth for read state, never purely local',
      'Push channel with a polling fallback so state cannot silently go stale',
      'Server-side monotonic ordering to avoid race conditions on mark-as-read',
      'Names what is instrumented (connect success, fallback rate, badge divergence) and the rollback trigger',
    ],
    followUps: ['How would you test the cross-tab sync without spinning up a real server?', 'What would you do differently for a notification that requires an action, like an approval?'],
  },
  {
    id: 'design-006',
    round: 'design',
    category: 'Product surfaces',
    question: 'Design a multi-step application wizard (e.g. a loan or visa application) that must let the user resume exactly where they left off.',
    answer: [
      "Treat each step's data as its own persisted unit, not one giant client-side object held in memory: autosave on blur or on a short debounce per field, so a crashed tab or a closed browser never loses more than a few seconds of input.",
      "Model progress explicitly on the server (current step, completed steps, validation state per step) rather than inferring it from what data happens to be filled in, since a user might fill step 3 before finishing step 2 during a save-and-resume flow.",
      "Cover step validation carefully: validate a step when the user tries to leave it, not only at final submit, so errors surface early, but allow navigating backward freely to fix earlier steps without re-validating everything ahead of the user's current position.",
      "Handle the resume entry point directly: on return, route the user to their last incomplete step by default, but always allow jumping to any earlier completed step from a visible progress indicator, since forcing a rigid replay is what actually loses users.",
      "Close on the non-functionals. Accessibility is a legal floor on a flow like this, not a nicety: a consumer loan application sits in the European Accessibility Act's consumer-banking scope — the BFSG in Germany, in force since June 2025 — and a public-sector visa portal is covered instead by the Web Accessibility Directive, so either way WCAG 2.2 AA, the level EN 301 549 tracks, is a compliance requirement. Form labelling, programmatic error identification, and keyboard-only completion are the parts that actually get audited. Then instrument per-step drop-off and autosave failure rate, ship step changes behind a flag, and roll back on a drop-off regression.",
    ],
    keyPoints: [
      'Per-field or per-step autosave, not one large in-memory object',
      'Server-side explicit progress model, not inferred from filled data',
      'Step-level validation on exit, not only at final submit',
      'Resumes at the last incomplete step with a visible way to jump back',
      'Names WCAG 2.2 AA via EN 301 549 and the EAA/BFSG as a legal floor for this flow, not a nicety',
      'Instruments per-step drop-off and autosave failure rate with a flagged rollout and rollback trigger',
    ],
    followUps: ['How would you handle a step whose validation depends on a slow external check, like an ID verification service?', 'How would you migrate in-progress applications if you change the step order later?'],
  },
  {
    id: 'design-019',
    round: 'design',
    category: 'Product surfaces',
    question: 'Design a multi-currency balance view: a user holds several currencies, and the UI must show live-converted totals without misleading anyone about exact amounts.',
    answer: [
      "Separate stored value from displayed value hard: the amount actually held in each currency is the source of truth and never changes based on a conversion rate; a converted total shown in a reference currency is a derived, clearly-labeled estimate, never presented as an exact balance.",
      "Handle precision correctly: hold monetary amounts as integers in the smallest unit (cents) or a decimal library, never as floating-point numbers, since float rounding errors in a balance screen are a real-world class of production bug, not a theoretical one.",
      "Make rate staleness visible: show when the conversion rate was last fetched, refresh it on an interval or on focus, and never let a stale rate silently sit for hours presented as current — a fintech user will notice and lose trust immediately.",
      "Cover the input side too: if the user is converting or transferring, lock in the rate at confirmation time and show a short countdown or re-confirmation if it expires before the action completes, rather than executing at a rate that has since moved.",
      "Close on the non-functionals: rate age is the thing to alert on — monitor feed staleness p95 and page if the last successful rate fetch is older than the threshold you display against, since a silently stale rate is worse than a visibly missing one. Ship a rate-source or display change behind a flag, and roll back on a rise in expired or failed conversions.",
    ],
    keyPoints: [
      'Stored per-currency amount is the source of truth; converted total is a labeled estimate',
      'Uses integer cents or a decimal library for money, never raw floats',
      'Shows rate staleness/last-updated time rather than presenting a rate as permanently current',
      'Locks a conversion rate at confirmation time with expiry handling for a real transaction',
      'Alerts on rate-feed staleness p95 and names a rollback trigger tied to expired or failed conversions',
    ],
    followUps: ['How would you handle a currency pair with no direct rate, requiring conversion through a third currency?', 'What would you show if the rate feed itself goes down?'],
  },
  {
    id: 'design-020',
    round: 'design',
    category: 'Product surfaces',
    question: 'Design a transaction history screen: thousands of rows, filterable by date/category/amount, searchable, and exportable to CSV.',
    answer: [
      "Set the budget before the architecture, since it decides where the work happens: first page of rows p95 under 1s, a filter change reflected in under 500ms, and INP under 200ms on the filter controls themselves. Those numbers rule out doing the work in the browser.",
      "Push filtering and search to the server, not the client: with thousands of rows the client should never hold the full dataset just to filter it locally — send filter/search/sort parameters and paginate the results with a cursor.",
      "Design the filter state as URL-shareable query parameters, not only component state, so a filtered view can be bookmarked, shared, or survive a refresh — a real, frequently-requested feature for exactly this kind of screen.",
      "Cover export honestly: a CSV export of a filtered, possibly large result set should be a server-generated async job with a download-when-ready notification, not a client-side loop fetching every page and building the file in the browser, which will time out or freeze the tab past a modest size.",
      "Address categorization UX: category is often user-editable after the fact (recategorizing a transaction), so design that as an inline, immediately-persisted edit rather than a separate screen, and make sure a recategorization updates any already-rendered summary totals.",
      "Close on the non-functionals. This is consumer banking, so accessibility is a legal floor rather than a nicety: the European Accessibility Act — the BFSG in Germany, in force since June 2025 — puts WCAG 2.2 AA, the level EN 301 549 tracks, on the compliance side of the line, and a data table is exactly where that bites (real table semantics, a keyboard-operable filter set, and an announced result count, not a silently re-rendered list). Then measure the budgets above in RUM, ship server-side filtering behind a flag against the current implementation, and roll back on a p95 or export-failure regression.",
    ],
    keyPoints: [
      'Filtering, search, and sort are server-side with cursor pagination, not client-side over a full dataset',
      'Filter state lives in the URL so views are shareable and refresh-safe',
      'Large exports are an async server job with a ready notification, not a client-side loop',
      'Inline recategorization updates any visible summary totals immediately',
      'States numeric budgets (first page p95 under 1s, filter under 500ms, INP under 200ms) up front',
      'Names WCAG 2.2 AA via EN 301 549 and the EAA/BFSG as a compliance floor, and what that means for a data table',
      'Measures those budgets in RUM with a flagged rollout and a named rollback trigger',
    ],
    followUps: ['How would you keep the running balance column correct while a filter is applied?', 'How would you handle a search term that matches a merchant name stored inconsistently across transactions?'],
  },
  {
    id: 'design-021',
    round: 'design',
    category: 'Product surfaces',
    question: 'Design the document-capture step of a KYC (identity verification) flow: photograph an ID document and a selfie, handle a rejected submission, and let the user retry.',
    answer: [
      "Scope this to the capture and verification UX specifically, not the whole onboarding wizard: guide the user to a good photo (in-frame guides, blur/glare detection before upload) since a bad capture causing a slow, generic rejection later is the most common real-world failure of these flows.",
      "Give live feedback before submission where feasible: a client-side check for obvious problems (image too dark, document out of frame, cropped) provides an instant retry loop, far faster than a round trip to a verification service that takes seconds to minutes.",
      "Design the rejection path to be specific, not generic: 'document expired' and 'photo unreadable' need different next steps shown to the user, and each retry should reset only the failed capture, not force restarting the entire step.",
      "Treat the images as highly sensitive personal data end to end: upload over TLS directly to a secure, access-controlled store (ideally not routed through general application logs or error trackers), and make sure any client-side preview of the captured image is cleared from memory/state once the upload completes.",
      "Close on the non-functionals: the metrics that matter are first-attempt pass rate and p95 capture-to-decision time — a falling pass rate is the signal that a change to the capture guidance regressed, and it is invisible unless you watch it. Ship a change to the quality checks behind a flag on a small traffic slice, and roll back on a pass-rate drop rather than arguing about screenshots.",
    ],
    keyPoints: [
      'Scoped to capture/verification UX, not the whole onboarding wizard',
      'Client-side pre-submission checks (blur, framing, glare) give an instant retry loop',
      'Specific, actionable rejection reasons per failure type, not a generic retry',
      'Treats captured images as sensitive personal data through upload, storage, and cleanup',
      'Names first-attempt pass rate and capture-to-decision p95 as the metrics, with a flagged rollout and rollback trigger',
    ],
    followUps: ['How would you support this on a low-end device with a poor camera?', 'How would you test the pre-submission image-quality checks without a real verification backend?'],
  },
  {
    id: 'design-022',
    round: 'design',
    category: 'Product surfaces',
    question: 'Design a live courier/delivery tracking view: a map showing a moving courier, an ETA that updates, and a route line.',
    answer: [
      "Name the budgets first, since they are what makes this feel live rather than laggy: under 100ms from a position update arriving to the repainted marker, INP under 200ms on pan and zoom, and LCP under 2.5s for the first meaningful map paint on a mid-range phone — the map tiles are the heavy part of that last one.",
      "Separate the update cadence from the render cadence: position updates might arrive every few seconds over a WebSocket, but you should interpolate the marker's movement smoothly between updates (a short client-side animation toward the new point) rather than snapping, which reads as broken.",
      "Treat the ETA as a derived, uncertain estimate and say so in the UI (a range or a 'about' prefix), recomputed on each position update from a routing service, rather than a single fixed number the user learns to distrust the moment it is wrong.",
      "Handle the map/route data volume deliberately: render only the relevant route segment near the courier's current position at high detail, and simplify the geometry for the parts already traveled, since a full precise polyline for a long route is unnecessary rendering cost.",
      "Cover the disconnect case: if position updates stop arriving, show a 'last seen' timestamp rather than freezing the marker with no indication anything is wrong, and reconnect the stream with backoff.",
      "Close on the non-functionals. Transport services are named in the European Accessibility Act — the BFSG in Germany, in force since June 2025 — so WCAG 2.2 AA, the level EN 301 549 tracks, is a compliance floor here and a map is the classic offender: the ETA, the courier status, and the address must all be available as text to a screen reader, not only as pixels on a canvas. Then measure the budgets above in RUM, alert on feed staleness, ship behind a flag, and roll back on a tick-to-paint or stream-disconnect regression.",
    ],
    keyPoints: [
      'Interpolates marker movement between updates instead of snapping',
      'Presents ETA as an explicit estimate/range, recomputed per update',
      'Simplifies already-traveled route geometry to control rendering cost',
      'Shows a last-seen indicator rather than silently freezing on a dropped stream',
      'States numeric budgets (update-to-paint under 100ms, INP under 200ms, LCP under 2.5s) before designing',
      'Names WCAG 2.2 AA via EN 301 549 and the EAA/BFSG, and gives the map a text equivalent for ETA and status',
      'Measures those budgets in RUM with a flagged rollout and a named rollback trigger',
    ],
    followUps: ['How would you handle multiple couriers on one map view (e.g. a dispatcher dashboard)?', 'How would you test the interpolation logic without a live GPS feed?'],
  },
  {
    id: 'design-023',
    round: 'design',
    category: 'Product surfaces',
    question: 'Design a live preview pane for a CMS: an editor on one side, and a preview of the rendered page on the other, updating as the user types.',
    answer: [
      "Decide the isolation boundary first, and be precise about what each part buys you. An iframe gives you a separate document, which is what stops the previewed page's CSS and DOM from leaking into the editor UI and vice versa. It does not by itself give you script isolation: a same-origin or srcdoc iframe shares an origin with the editor, so script inside it can reach the parent through window.parent and do whatever the editor can do.",
      "So if the previewed content is untrusted — author-supplied HTML, third-party embeds — the actual controls are the sandbox attribute (sandbox without allow-same-origin, adding back only the capabilities the preview genuinely needs) and serving the preview from a distinct origin, so the same-origin policy is doing real work rather than being assumed. For a trusted, first-party template, a plain iframe for style isolation is the proportionate answer, and I would say which case I am designing for.",
      "Communicate editor-to-preview changes via postMessage rather than direct DOM manipulation across the iframe boundary, since that is the only safe, standard channel between two separate browsing contexts, and validate the message shape on the receiving side rather than trusting it blindly.",
      "Debounce the update, not the keystroke handling: batch rapid typing into a preview refresh every 200-400ms rather than re-rendering the full preview on every keystroke, which is wasted work and can visibly stutter for a content-heavy page.",
      "Preserve preview state across updates where it matters: a full iframe reload on every change loses scroll position and any interactive state (an open accordion, a video mid-play) — prefer patching the rendered content in place, or explicitly restoring scroll position after a reload if a full reload is simpler to build first.",
      "Close on the non-functionals: the felt metric is keystroke to updated preview, so hold that under 400ms at p95 and measure it, alongside an error rate for preview renders that throw. Ship the in-place patching path behind a flag with full reload as the fallback, and roll back if preview error rate rises.",
    ],
    keyPoints: [
      'Separates document isolation from origin isolation: says a same-origin/srcdoc iframe does not stop scripts',
      'Names sandbox without allow-same-origin plus a distinct origin as the control for untrusted preview content',
      'Editor-to-preview communication goes through postMessage with validated payloads',
      'Debounces preview refresh instead of re-rendering on every keystroke',
      'Preserves or restores scroll/interactive state across preview updates',
      'Names a keystroke-to-preview budget (p95 under 400ms) plus a flagged rollout with full reload as the fallback',
    ],
    followUps: ['How would you support a preview of a responsive breakpoint without a real device?', 'How would you validate a postMessage payload against a malicious or buggy sender?'],
  },
  {
    id: 'design-024',
    round: 'design',
    category: 'Product surfaces',
    question: 'Design a fraud/risk review queue: an analyst works through flagged transactions, viewing evidence side by side and recording a decision with an audit trail.',
    answer: [
      "Design the case view around comparison, not a single record: the analyst needs the flagged transaction alongside relevant context (recent transaction history, device/location signals, prior flags on this account) visible at once, not behind separate tabs they must reconstruct mentally.",
      "Make every decision an explicit, structured action, not a free-text note: approve/deny/escalate with a required reason code, so the data is later queryable (which reason codes correlate with confirmed fraud) rather than locked in prose.",
      "Treat the audit trail as non-negotiable and append-only: every view, decision, and reason is logged with who and when, and a decision is never silently editable after the fact — a correction is a new logged entry referencing the original, not an overwrite, since this data has real compliance and legal weight.",
      "Design for queue fairness and throughput explicitly: show the analyst their queue position/count, prevent two analysts from being assigned the same case simultaneously (a claim/lock mechanism), and support a fast keyboard-driven flow for the common path, since analysts triage a high volume of cases per shift.",
      "Close on the non-functionals: the metrics are median time-to-decision and queue age p95, with an alert when the oldest unclaimed case crosses the review SLA — a fraud queue that silently backs up is a real financial loss, not a UX complaint. Roll UI changes out to a slice of analysts behind a flag, watch decision-reversal rate as the quality signal, and roll back if it rises.",
    ],
    keyPoints: [
      'Side-by-side case context, not scattered across separate views',
      'Decisions are structured (reason codes), not free text, for later analysis',
      'Audit trail is append-only; corrections are new entries, never silent edits',
      'Explicit case-locking and a fast keyboard flow for analyst throughput',
      'Names time-to-decision and queue-age p95 with an SLA alert, and decision-reversal rate as the rollback trigger',
    ],
    followUps: ['How would you handle two analysts disagreeing on the same case?', 'How would you surface a pattern across many cases (e.g. the same device flagged repeatedly) to the analyst reviewing one of them?'],
  },

  {
    id: 'design-028',
    round: 'design',
    category: 'Product surfaces',
    question: 'Design a real-time order book for a trading screen: a price ladder of bids and asks fed by a market data stream delivering hundreds of updates a second.',
    answer: [
      "Budget first, because every later decision falls out of it: tick-to-paint under 100ms from a message arriving to the top of book being on screen, INP under 200ms for the interactions layered on top (clicking a level to prefill an order), and no dropped frames during a burst. Name the number before you name the technology.",
      "Get the data contract right: the feed is a snapshot plus a stream of deltas, each carrying a monotonically increasing sequence number. The client applies deltas to an in-memory book keyed by price level. A gap in sequence numbers is not something you paper over — you discard the local book, re-request a snapshot, and replay the deltas you buffered from that snapshot's sequence forward. Silently continuing from a gapped book is how a screen ends up confidently wrong.",
      "Separate render cadence from message cadence: coalesce every message arriving within a frame into one mutation of the local book, then paint once per animation frame from the latest state. At several hundred updates a second that is the difference between roughly 16 paints and hundreds, and no human can read a price that changes 300 times a second anyway.",
      "Virtualise the ladder: render only the visible price levels plus a small buffer, key each row by price so a level keeps its DOM node as its quantity changes, and keep levels in a structure you can splice rather than re-sorting the whole book per tick. Keep the hot path out of React state — write changing quantities straight to refs or a canvas, and reserve React for the parts the user actually interacts with.",
      "Staleness is the safety requirement, not a polish item: never render a stale price as live. Heartbeat the connection, and if the sequence gaps or the newest update is older than a second or two, grey the ladder, show an explicit delayed/reconnecting state, and disable order entry against those prices until a fresh snapshot lands. Reconnect with jittered backoff and resume from snapshot plus deltas, never by replaying the session from the start.",
      "Close on the non-functionals: report tick-to-paint p95 and dropped-frame rate as RUM metrics from real sessions, alert on feed staleness and gap rate, ship the new ladder behind a flag with the current one as the rollback, and roll back on a budget breach or any instance of a stale price rendering as live.",
    ],
    keyPoints: [
      'Names a tick-to-paint number (under 100ms) and an INP budget before designing anything',
      'Snapshot plus sequenced deltas; a sequence gap forces a fresh snapshot rather than a patched-over book',
      'Coalesces a burst into one mutation per animation frame instead of rendering per message',
      'Virtualises the price ladder, keys rows by price, and keeps the hot path out of React state',
      'Greys the ladder and disables order entry on a stale feed rather than showing stale prices as live',
      'Reconnects with backoff and resumes via snapshot plus deltas, not a full replay',
      'Reports tick-to-paint p95 in RUM with a flagged rollout and a stale-render rollback trigger',
    ],
    followUps: ['How would you prove the coalescing never drops the final update of a burst?', 'What changes if the same screen has to show twenty instruments at once instead of one?'],
  },

  // Platform & scale (10)
  {
    id: 'design-007',
    round: 'design',
    category: 'Platform & scale',
    question: 'Design a frontend that serves many tenants with distinct branding and feature sets from one codebase.',
    answer: [
      "Separate three concerns that get conflated in a bad design: visual theming (colors, logo, fonts), feature availability (which modules/routes exist for this tenant), and data isolation (a tenant must never see another tenant's data) — each needs its own mechanism.",
      "Theming should be data, not code: a tenant config resolved at load time (by subdomain or auth claim) drives CSS custom properties and asset URLs, so adding a tenant never means a deploy.",
      "Feature availability should be a capability check at the route and component level, backed by the same tenant config, not scattered environment checks or hardcoded tenant-id conditionals that rot the moment a new tenant needs a slightly different mix.",
      "Data isolation is a backend contract the frontend must respect defensively: always scope requests by the resolved tenant id from the session, never trust a tenant id passed in the URL or a client-editable field, and treat any endpoint that skips this as a blocking bug, not a nice-to-have.",
      "Only if asked: cover testing explicitly — CI exercises at least one representative tenant configuration beyond the default, since a change that is only tested against one tenant can silently break another tenant's theme or feature combination.",
      "Close on the non-functionals: RUM has to be sliced per tenant, or a regression in one tenant's theme or bundle reaches you as a support ticket from that tenant rather than as a metric. Roll tenant-config changes out per tenant behind a flag, with the previous config as a one-step rollback.",
    ],
    keyPoints: [
      'Theming, feature availability, and data isolation treated as three separate mechanisms',
      'Tenant config resolved at load time drives theming without a deploy per tenant',
      'Feature flags are capability checks against tenant config, not scattered conditionals',
      'Tenant scoping always comes from the session, never from client-editable input',
      'CI covers more than one representative tenant configuration',
      'Slices RUM per tenant and rolls config changes out per tenant with a one-step rollback',
    ],
    followUps: ['How would you let one tenant test a beta feature before it ships to everyone?', 'How would you structure this if two tenants needed genuinely different page layouts, not just colors?'],
  },
  {
    id: 'design-008',
    round: 'design',
    category: 'Platform & scale',
    question: 'Design a frontend for 20+ locales, including at least one right-to-left language.',
    answer: [
      "Start with what breaks first at scale: string concatenation. Every user-facing string must go through an i18n library with proper interpolation and pluralization rules (many languages have more than the two English plural forms), never built by joining translated fragments.",
      "Cover layout direction explicitly: RTL is not just mirrored text, it is mirrored layout — use logical CSS properties (margin-inline-start instead of margin-left) from the start rather than retrofitting them, since a retrofit touches nearly every component.",
      "Cover the operational side: translations live outside the codebase (a TMS or structured files pulled at build or runtime), missing keys fall back visibly in a way translators can catch, not silently to English in production, and dates/numbers/currency format via the platform's locale APIs, not manual formatting.",
      "Address performance: do not ship all 20+ locale bundles to every user — load only the active locale, and treat locale-switching as a first-class, testable flow rather than an edge case.",
      "Close on the non-functionals: report locale bundle size and the missing-key rate per locale from production, since a locale nobody on the team reads is exactly where an untranslated string survives for months. Ship a new locale behind a flag to internal users first, and treat an RTL layout regression as a rollback trigger rather than a follow-up ticket.",
    ],
    keyPoints: [
      'All user-facing strings go through an i18n library with real pluralization support',
      'Logical CSS properties used from the start, not retrofitted for RTL',
      'Locale files decoupled from the codebase with a visible fallback for missing keys',
      'Only the active locale bundle ships to a given user',
      'Reports missing-key rate and bundle size per locale from production, with an RTL regression as a rollback trigger',
    ],
    followUps: ['How would you catch a hardcoded English string before it reaches production?', 'How would you test RTL layout in CI without a human reviewing every screen?'],
  },
  {
    id: 'design-009',
    round: 'design',
    category: 'Platform & scale',
    question: 'Design a permissions-aware UI where the same screen renders differently depending on the viewer\'s role.',
    answer: [
      "Model permissions as data the client receives, not logic the client invents: the backend returns the resolved set of capabilities for the current user on this resource, and the frontend only branches on that set — it never re-derives 'is this user an admin' from role names scattered through the UI.",
      "Design components to accept a capability, not a role: a button checks canEdit, not role === 'admin', so a new role that also happens to get edit rights needs zero UI changes.",
      "Treat hiding as UX, not security: a hidden button is a convenience for the wrong-permission user, not a security boundary — the backend must independently reject the action, and the frontend should assume every request can be attempted directly regardless of what is rendered.",
      "Cover the harder case: partial visibility within a single view (e.g. some fields visible, some redacted) — model that at the field level in the same capability-driven way, so redaction is not a special-cased UI hack layered on top of a full-access component.",
      "Close on the non-functionals: log both client-side capability denials and server-side rejections of actions the UI had already hidden — a spike in the second means the two models have drifted apart, which is the failure you actually care about. Roll permission-model changes out behind a flag, and roll back on a rise in unexpected 403s.",
    ],
    keyPoints: [
      'UI branches on backend-resolved capabilities, never on re-derived role logic',
      'Components check a capability, not a role name',
      'Hiding an action in the UI is never treated as the actual security boundary',
      'Field-level redaction uses the same capability model as whole-view branching',
      'Alerts on unexpected 403s as the signal that client and server permission models have drifted',
    ],
    followUps: ['How would you test that a hidden action is also properly rejected server-side?', 'How would you handle a permission that changes while the user has the page open?'],
  },
  {
    id: 'design-010',
    round: 'design',
    category: 'Platform & scale',
    question: 'Design the rollout of a breaking design-system change across dozens of consuming apps without a big-bang cutover.',
    answer: [
      "Reframe the goal: this is a migration problem, not a release problem — a single version bump that changes a public API or visual contract for dozens of apps at once is the failure mode to design around.",
      "Ship the new behavior additively first: introduce the new API or component variant alongside the old one under a clearly named prop, so consumers can adopt on their own schedule instead of being forced by a version bump.",
      "Make the blast radius visible: instrument which apps and how many call sites use the changing component, so you can sequence outreach from lowest-risk to highest-risk consumers and know when it is actually safe to deprecate.",
      "Provide a codemod wherever the change is mechanical, set a real deprecation deadline communicated up front, and track the long tail of stragglers actively rather than assuming a changelog entry is enough — the rollout is not done until adoption is, not until the code ships.",
    ],
    keyPoints: [
      'Names a migration path with a deprecation deadline rather than a single version bump',
      'New behavior ships additively alongside the old, not as a forced breaking change',
      'Usage is instrumented so blast radius and rollout order are known, not guessed',
      'Codemod plus an active, tracked deprecation deadline rather than a changelog note',
    ],
    followUps: ['How would you handle an app that cannot migrate before your deadline?', 'What would you automate first to make the next breaking change cheaper?'],
  },
  {
    id: 'design-011',
    round: 'design',
    category: 'Platform & scale',
    question: 'Design a frontend that keeps working through a dropped network mid-session (offline-first).',
    answer: [
      "Decide what offline actually means for this product before architecting anything: read-only access to cached data, or full read-write with sync-on-reconnect — these need very different amounts of complexity, so scope this explicitly rather than defaulting to the hardest version.",
      "For the caching layer, use a service worker to cache the app shell and recent data responses, so the app still loads and shows something useful, not a blank screen or a browser error page, when the network is gone.",
      "For read-write offline, queue mutations locally (an outbox pattern) with enough context to replay them on reconnect, and design for conflict explicitly: last-write-wins is fine for some data, but anything collaborative needs a real merge or a conflict UI, not a silent overwrite.",
      "Only if asked: make connectivity state visible and honest — show the user when they are offline and when a queued action is pending sync, rather than letting the UI silently pretend everything succeeded while a mutation sits unsent in a queue.",
      "In an EU/GDPR context, treat local persistence of personal data deliberately: cache only what the offline flow genuinely needs, give queued data a bounded lifetime, and make sure a user-initiated logout or data deletion actually clears the local store rather than leaving stale personal data on the device.",
      "Close on the non-functionals: instrument outbox depth, sync success rate, and time-to-sync after reconnect — an outbox that quietly grows is the offline failure mode nobody notices until a user complains. Ship the offline path behind a flag to a small cohort first, and roll back on a rise in failed or conflicting syncs.",
    ],
    keyPoints: [
      'Scopes offline support explicitly (read-only cache vs full read-write) before designing',
      'Service worker caches the app shell so offline never means a blank screen',
      'Outbox pattern for queued mutations with an explicit conflict strategy',
      'Connectivity and pending-sync state are visible to the user, never silent',
      'Addresses GDPR data-minimization and clearing local personal data on logout/deletion',
      'Instruments outbox depth and time-to-sync, with a cohort rollout and a failed-sync rollback trigger',
    ],
    followUps: ['How would you handle a queued mutation that fails validation once it finally syncs?', 'How would you test the offline path in CI?'],
  },
  {
    id: 'design-012',
    round: 'design',
    category: 'Platform & scale',
    question: 'Design a real-time collaborative editor with multiple concurrent cursors and edits.',
    answer: [
      "Name the hard problem directly: concurrent edits to the same document need a conflict-free merge strategy, not last-write-wins, so this is the one area where reaching for an established approach (CRDTs or operational transformation) beats inventing your own.",
      "Separate presence from content: cursor positions and selections are ephemeral, high-frequency, and can be dropped safely (send over a lightweight channel, no persistence needed), while document edits are the durable, must-not-lose data and need reliable delivery and eventual persistence.",
      "Design the client to apply local edits optimistically and immediately, then reconcile with the merged state from the server/peers as it arrives — the CRDT or OT library handles the merge math, but the UI still needs to smoothly reposition remote cursors as the document shifts under them.",
      "Cover the reconnect case: on a dropped connection, the client must be able to resync to the current document state (not replay every historical operation) and rejoin presence cleanly, so a flaky connection degrades to 'catch up once' rather than a corrupted document.",
      "Close on the non-functionals: the felt budgets are local keystroke-to-paint under 50ms (a local edit must never wait on the network) and remote edit-to-visible under 200ms at p95. Instrument both plus resync frequency, ship behind a flag with the previous single-writer path as the rollback, and roll back on any report of document divergence, since a corrupted document is not something you burn down gradually.",
    ],
    keyPoints: [
      'Reaches for CRDTs/OT rather than inventing a custom merge strategy',
      'Separates ephemeral presence data from durable document edits',
      'Applies local edits optimistically, reconciling with merged state as it arrives',
      'Reconnect resyncs to current state rather than replaying full history',
      'Names latency budgets (local edit under 50ms, remote edit visible under 200ms p95) and a divergence rollback trigger',
    ],
    followUps: ['How would you handle a user who has been offline for an hour rejoining the session?', 'How would you show attribution for who wrote what without cluttering the document?'],
  },
  {
    id: 'design-025',
    round: 'design',
    category: 'Platform & scale',
    question: 'Design a resumable file upload for large files (e.g. bank statement PDFs or exported data) that must survive a dropped connection or a closed laptop lid.',
    answer: [
      "Chunk the file client-side and upload it in fixed-size pieces, tracking which chunks have been acknowledged by the server, so a resume only needs to send the remaining chunks rather than restarting the whole file.",
      "Persist upload progress somewhere that survives a closed tab: an IndexedDB record (or, for a short interruption, an in-memory retry) mapping file identity (name, size, a content hash) to acknowledged chunk indices, so reopening the tab can detect and resume a matching in-progress upload.",
      "Make chunk upload idempotent: each chunk request includes its index and the upload's id, so a chunk re-sent after an ambiguous network failure (request sent, response lost) does not get applied twice server-side.",
      "Cover the finish condition explicitly: the server assembles chunks only after an explicit 'complete' call once every chunk is acknowledged, with a checksum comparison against the original file, so a subtly corrupted upload is caught before being treated as done.",
      "Close on the non-functionals: hold p95 feedback on a chunk under 1s so progress never looks frozen even when throughput is poor, instrument resume rate and chunk-retry rate, ship chunked upload behind a flag with single-request upload as the fallback, and roll back on a rise in checksum mismatches.",
    ],
    keyPoints: [
      'Uploads in fixed-size, independently-acknowledged chunks, not one request for the whole file',
      'Persists progress (e.g. IndexedDB) keyed by file identity so a resume can be detected',
      'Chunk uploads are idempotent against ambiguous network failures',
      'Explicit complete step with a checksum check before treating the upload as done',
      'Names a p95 chunk-feedback budget under 1s, plus a flagged rollout with single-request upload as the fallback',
    ],
    followUps: ['How would you detect that a resumed upload is actually for a different, similarly-named file?', 'How would you show upload progress accurately across pause/resume cycles?'],
  },
  {
    id: 'design-026',
    round: 'design',
    category: 'Platform & scale',
    question: 'Design a feature-flag and experimentation platform for the frontend: flags can target a percentage of users, specific cohorts, and support instant kill-switch rollback.',
    answer: [
      "Separate flag evaluation from flag management: the client fetches a resolved, evaluated set of flags for the current user (given their id/cohort attributes) from an edge-cached endpoint, rather than downloading the full targeting-rule engine and evaluating rules client-side, which leaks rollout logic and is slower.",
      "Resolve the tension between kill-switches and consistency by making the kill-switch its own flag class with its own delivery path. Ordinary flags resolve once per page load and are then frozen for the rest of the session, so a user never sees a feature flip on and off mid-flow and a targeting change lands on their next load. Kill-switches are the deliberate exception: they ride a dedicated channel — a push invalidation, or a poll on a few-second TTL — that force-re-resolves just those flags mid-session, because the entire value of a kill-switch is disabling a broken feature in seconds rather than on the next deploy.",
      "That exception has a cost you should name: any feature behind a kill-switch has to be able to tear itself down cleanly mid-session — unmount, stop its timers, and leave the user somewhere coherent rather than half in a flow that no longer exists. If a feature cannot do that (a payment already in flight, say), the kill-switch gates entry to the flow instead of ripping it out underneath the user.",
      "Address the code-hygiene failure mode directly: every flag needs an owner and an expected removal date, since the classic outcome of a flag system is permanent 'temporary' branches — treat an untouched flag past its expected date as tech debt to clean up, not a permanent architecture.",
      "Close on the non-functionals: the flag platform needs its own observability — latency and error rate on the resolve endpoint (it is now on every page load's critical path, so it needs a cached default to fail open to), plus per-flag exposure counts so you can prove a rollout actually reached the cohort you targeted. Where you can, wire the kill-switch to an automatic trigger on an error-rate or crash-rate threshold for the flagged cohort, so rollback does not depend on someone being awake.",
    ],
    keyPoints: [
      'Client fetches pre-evaluated flags from an edge-cached endpoint, not the full rule engine',
      'Says the kill-switch is a separate flag class that force-re-resolves mid-session via push or a short-TTL poll',
      'Says all other flags freeze at page load, so a targeting change lands on the next load',
      'Names the cost of the exception: a kill-switchable feature must tear down cleanly, or the switch gates entry instead',
      'Every flag has an owner and expected removal date to avoid permanent flag debt',
      'Treats the resolve endpoint as critical path: fails open to a cached default and reports per-flag exposure counts',
      'Wires the kill-switch to an automatic error-rate trigger rather than relying on a human being awake',
    ],
    followUps: ['How would you test a flag combination locally without hitting the real targeting service?', 'How would you roll out a flag gradually while watching for a specific error-rate regression?'],
  },

  {
    id: 'design-027',
    round: 'design',
    category: 'Platform & scale',
    question: 'A monolithic SPA has grown until six teams ship on one shared release train. Design a migration to independently deployable frontends (Module Federation or similar) — or argue against it.',
    answer: [
      "Start by naming the real driver, because it is organisational, not technical: micro-frontends buy deploy independence for teams that are currently blocking each other. If the pain is a shared release train and a shared regression suite, they address it. If the stated pain is bundle size or code organisation, route-level code splitting and enforced module boundaries get most of the win at a fraction of the cost, and I would say that out loud before agreeing to split anything.",
      "Split along ownership boundaries that match user-visible surfaces — whole routes or whole products, never horizontally by layer — and keep the host-to-remote contract deliberately narrow: a mount and unmount function, a route prefix it owns, and a small set of events. Anything richer than that is a distributed API you now have to version across independently deployed artefacts.",
      "Name the costs honestly, because this is where the answer separates from a slide deck. Shared dependencies become singletons you have to police (React and the router shared, with a version-skew policy and a build-time check, or you ship two Reacts). The design system drifts once each remote pins its own version, so it stays a single shared singleton with additive-only breaking changes. Auth and session live in the host and are passed down — remotes never fetch their own tokens. The host owns the URL, remotes own their subtree. And there is a real performance bill: duplicated vendor code, an extra request per remote, and a worse critical path unless you preload the remote for the current route.",
      "Phase it as a strangler fig rather than a cutover: the existing monolith becomes the host, one low-risk, high-autonomy route moves out first behind a flag, and both implementations stay shippable until the new one has run in production for a week. Migrate the second route only once the first team is genuinely deploying on its own schedule — if they are not, the split is not paying for itself and should stop there.",
      "Be explicit about what stays in the monolith: the design system, the auth and session shell, and the cross-cutting clients (analytics, error reporting, feature flags). Those are libraries with one owner, and shipping them as independently deployable remotes buys version skew and no autonomy at all. Close on the measure that justifies the whole exercise — deploys per team per week and lead time to production, tracked alongside LCP, INP, and total transferred JS per route. If the deploy metrics have not moved within a quarter while the performance ones have got worse, that is the trigger to stop and consolidate back, not to finish the migration for its own sake.",
    ],
    keyPoints: [
      'Names team topology and deploy independence as the driver, and says when route-level code splitting is the cheaper answer',
      'Splits vertically by route or product ownership, never horizontally by layer, with a narrow mount/unmount contract',
      'Names the specific costs: shared-dependency singletons, design-system drift, host-owned auth and routing, duplicated vendor JS',
      'Phases it strangler-fig: one route at a time behind a flag, both paths shippable',
      'Names what stays a library rather than a remote (design system, auth shell, analytics, flag client)',
      'Ties the migration to measured deploys-per-team and lead time, with a stop-and-consolidate trigger',
    ],
    followUps: ['How do you stop two remotes on different React versions from shipping two copies of React to the user?', 'What do you do when one team stops upgrading the shared design-system singleton?'],
  },
  {
    id: 'design-029',
    round: 'design',
    category: 'Platform & scale',
    question: 'Design the rendering and bundle architecture for a product with a public marketing site and a large authenticated app, where personal data must stay in the EU.',
    answer: [
      "Decide per surface, not per codebase — picking one rendering mode for the whole product is the actual mistake here. Public, indexable, first-visit-dominated pages want static generation or server rendering behind a CDN: real HTML for crawlers and social previews, with LCP under 2.5s on a mid-range phone as the budget. The authenticated app is the opposite — personalised, non-indexable, and visited repeatedly within a session — so a server-rendered shell for first paint plus client rendering after it, with INP under 200ms as the budget you actually defend.",
      "Treat data residency as a first-class NFR rather than a deployment detail, because SSR and edge rendering are data processing. An edge runtime that replicates to every point of presence worldwide will render EU users' personal data outside the EU unless you pin it, so either restrict rendering of authenticated, personal-data routes to EU regions, or keep those routes client-rendered against EU-hosted APIs and reserve the edge for anonymous, cacheable surfaces. Name the adjacent version of the same problem too: third-party scripts and your RUM and error-reporting endpoints are processors as well, and a non-EU analytics tag on an authenticated page is the same issue in a smaller package.",
      "Split the bundle at the route boundary first, since that maps to what a user actually loads, and only below that where a heavy dependency sits behind an interaction — a charting library, a rich text editor, a PDF viewer. Set a per-route JS budget and fail CI when it regresses, because a budget nobody enforces is a wish.",
      "Keep three caching layers distinct in your head and in the design: content-hashed static assets cached immutably for a year at the CDN; HTML cached only where it is anonymous, with stale-while-revalidate, and never for authenticated routes; and API data cached client-side with explicit invalidation on mutation. The dangerous mistake is caching a personalised HTML response at a shared CDN, which is how one user ends up seeing another user's page — worth stating explicitly, because it is a real incident class, not a hypothetical.",
      "Close on the non-functionals: measure this as field data, not lab data — RUM for LCP, INP, and CLS broken down per route, bundle size per route enforced in CI, and an error budget you actually spend. Change one route's rendering mode at a time behind a flag, compare against the old mode on real traffic, and roll back on an LCP or INP regression past the budget rather than debating it in a review.",
    ],
    keyPoints: [
      'Decides rendering mode per route with named budgets (LCP under 2.5s public, INP under 200ms in-app), not one mode everywhere',
      'Says SSR and edge rendering are data processing, and pins EU-only rendering or keeps personal-data routes client-rendered against EU APIs',
      'Names third-party scripts and RUM/error-reporting endpoints as processors subject to the same constraint',
      'Splits at route boundaries with a per-route JS budget enforced in CI',
      'Separates immutable asset caching, anonymous-HTML caching, and client data caching, and never caches personalised HTML at a shared CDN',
      'Changes rendering mode one route at a time behind a flag with an LCP/INP rollback trigger',
    ],
    followUps: ['How would you decide whether a given authenticated route is worth server-rendering at all?', 'What would you check before adding a third-party script to an authenticated page?'],
  },

  // Running the round (6)
  {
    id: 'design-013',
    round: 'design',
    category: 'Running the round',
    question: 'How do you spend the first five minutes of a 45-minute frontend system design interview?',
    answer: [
      "Resist the urge to start drawing boxes. Spend the first minutes on requirements: who are the users, what is the core interaction, what scale (users, data volume, update frequency) actually matters, and what is explicitly out of scope for this session.",
      "State the skeleton you will walk through out loud, so the interviewer can steer you: requirements, then data/API shape, then component tree, then state ownership, then performance, then accessibility/i18n, then observability and rollout. Naming it up front signals structure and gives the interviewer natural points to redirect you.",
      "Ask one or two sharpening questions rather than a long interrogation — staff-level candidates are expected to make reasonable assumptions and state them, not stall the round gathering requirements that do not change the design.",
      "Write the requirements and constraints somewhere visible (whiteboard corner or a running list) so you and the interviewer can refer back to them when a later trade-off decision needs grounding.",
    ],
    keyPoints: [
      'States requirements and scope before drawing anything',
      'States the answer skeleton out loud up front',
      'Asks a small number of sharpening questions, not an open-ended interrogation',
      'Keeps requirements visible for later reference',
    ],
    followUps: ['What do you do if the interviewer gives almost no requirements at all?', 'How much time is too much time on this phase?'],
  },
  {
    id: 'design-014',
    round: 'design',
    category: 'Running the round',
    question: 'The interviewer gives you a deliberately vague prompt. How do you turn it into something you can actually design?',
    answer: [
      "Recognize the vagueness as intentional: the interviewer wants to see how you scope an ambiguous problem, not guess the one 'correct' interpretation, so treat this as the actual skill being tested, not an obstacle to get past.",
      "Narrow it yourself with a stated assumption: pick one concrete interpretation ('I will design this for a B2C mobile-first audience with roughly a million daily active users') and say it out loud, inviting a correction rather than waiting for one.",
      "If the interviewer stays silent, proceed on your stated assumption — that silence is often deliberate, and stalling here reads worse than picking a reasonable scope and moving.",
      "Keep the scoped assumption visible throughout, and if a later requirement genuinely contradicts it, say so explicitly and adjust rather than quietly designing around the contradiction.",
    ],
    keyPoints: [
      'Says the vagueness is deliberate and scopes it, rather than hunting for the intended answer',
      'Narrows scope with a stated, concrete assumption instead of asking open-ended questions',
      'Proceeds confidently on the stated assumption if the interviewer does not correct it',
      'Flags explicitly if a later requirement contradicts the original assumption',
    ],
    followUps: ['Give an example of a scoping assumption you would state for a "design a chat app" prompt.', 'How do you avoid over-narrowing and missing what the interviewer actually wanted?'],
  },
  {
    id: 'design-015',
    round: 'design',
    category: 'Running the round',
    question: 'You are 30 minutes in and realize your initial approach does not scale to the stated requirement. What do you do live?',
    answer: [
      "Say it out loud immediately rather than quietly patching around it: 'I want to flag that this approach breaks down at the scale we discussed, let me adjust' — staff-level signal is catching your own mistake, not never making one.",
      "Isolate exactly what breaks: is it the data model, the rendering approach, or the state management, since a full restart is rarely needed — most scaling problems are fixable by swapping one layer, not redesigning the whole system.",
      "State the fix and its trade-off concisely, then keep moving: do not spend five minutes re-deriving the whole design from scratch, reuse what was already correct and patch the piece that was not.",
      "If time is genuinely too short to finish after the correction, say so and prioritize: 'given the time left, I will focus on X and summarize Y at a higher level' — an honest scope cut reads better than a rushed, incomplete redesign presented as finished.",
    ],
    keyPoints: [
      'Surfaces the mistake explicitly rather than silently patching around it',
      'Isolates the specific broken layer instead of restarting the whole design',
      'States the fix and trade-off concisely rather than re-deriving everything',
      'Makes an explicit, honest scope cut if time is genuinely short',
    ],
    followUps: ['How would you recover if the interviewer points out the flaw before you notice it yourself?', 'What is a scaling mistake you have actually made in a real design?'],
  },
  {
    id: 'design-016',
    round: 'design',
    category: 'Running the round',
    question: 'How do you decide what to draw on the whiteboard versus what to just say out loud?',
    answer: [
      "Draw structure, say reasoning: component trees, data flow between systems, and sequence of calls are much clearer as a diagram; trade-off reasoning ('I chose X over Y because...') almost never needs to be drawn and slows you down if you try.",
      "Keep the diagram loose and evolving rather than polished: a staff-level review values the reasoning behind the boxes far more than clean box alignment, so do not burn time redrawing for tidiness.",
      "Label things as you go, even roughly, so the diagram stays legible to the interviewer without you narrating every element from scratch when you refer back to it later in the round.",
      "When a diagram would take too long relative to its value (e.g. a detailed sequence diagram for a minor edge case), say so explicitly and describe it verbally instead, showing you are managing your own time deliberately.",
    ],
    keyPoints: [
      'Diagrams structure and data flow; explains trade-off reasoning verbally',
      'Keeps diagrams loose and evolving rather than spending time on polish',
      'Labels elements as they are drawn for later reference',
      'Explicitly chooses verbal explanation when a diagram is not worth the time',
    ],
    followUps: ['How do you handle a remote round where you cannot physically draw?', 'What is the one diagram you would always draw regardless of time pressure?'],
  },
  {
    id: 'design-017',
    round: 'design',
    category: 'Running the round',
    question: 'Halfway through, the interviewer asks "what if this had to handle 10x the traffic?" How do you respond?',
    answer: [
      "Treat it as a targeted follow-up, not a request to redesign everything: identify which specific part of your design was sized for the original scale (a particular data fetch, a rendering approach, a polling interval) and reason about that piece first.",
      "Walk the failure mode explicitly: what actually breaks at 10x (a naive client-side filter over a growing list, a polling interval that now floods the backend, a bundle that was fine for a small feed but chokes at this volume) before proposing the fix, so the fix is clearly justified rather than a reflexive 'add caching'.",
      "Propose a concrete, scoped change (virtualization, moving from polling to push, server-side pagination) and briefly state its own new trade-off, since every scaling fix introduces one.",
      "Close by stating what you would monitor to know if this fix is actually working in production, which shows you think about scale as an ongoing property, not a one-time design decision.",
    ],
    keyPoints: [
      'Identifies the specific component that breaks at 10x rather than redesigning wholesale',
      'States the concrete failure mode before proposing a fix',
      'Proposes a scoped fix and names its new trade-off',
      'Names what they would monitor to verify the fix in production',
    ],
    followUps: ['What if the interviewer then asks about 100x?', 'How do you avoid over-engineering for a scale that was never actually stated as a requirement?'],
  },
  {
    id: 'design-018',
    round: 'design',
    category: 'Running the round',
    question: 'How do you close out the round in the last five minutes?',
    answer: [
      "Summarize the design in under a minute: the core architecture, the two or three biggest trade-offs you made, and why you made them — the interviewer has been listening for 40 minutes and a tight summary shows you can also communicate to someone who was not in the room.",
      "Proactively name what you did not get to or would revisit with more time (e.g. 'I would go deeper on the caching invalidation strategy and on monitoring'), since staff-level self-awareness about the incomplete parts reads better than pretending the design is fully finished.",
      "If time allows, ask a genuine question back — about how the team actually solved this problem, or what the interviewer thought was the hardest part — which signals real engagement rather than relief that the round is over.",
      "Do not introduce a brand new idea in the last minute unless directly asked; closing is for consolidating what you already built, not for opening a new thread you cannot finish.",
    ],
    keyPoints: [
      'Summarizes architecture and top trade-offs concisely',
      'Proactively names what was left unaddressed rather than implying completeness',
      'Asks a genuine question back if time allows',
      'Introduces no new idea in the final minute; consolidates what is already built',
    ],
    followUps: ['What is a good question to ask the interviewer at this point?', 'How do you handle it if you genuinely ran out of time before reaching state management or performance at all?'],
  },
];
